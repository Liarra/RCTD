Opentip.styles.donateHint = {
    target: true, // Takes the <a /> element as target
    autoOffset: false,
    stem: false,

    tipJoint: "bottom", // So the tooltip floats above the link
    offset: [0, 50],

    background:"#ffffff",

    showEffect:"SlideDown",
    showEffectDuration:0.1,
    borderWidth:0,
    borderRadius:0,
    margin:0,
    style:"pointer-events:none",
    group: "content" // Ensures that only one tag Opentip is visible
};

opentip

